import theme from '@theme/index';
import {themeProps} from './types';

const imageTheme: themeProps = {
    borderRadius: 7,
    borderColor: theme.color.neutral.typography.black60
};

export default imageTheme;
