import theme from '@theme/index';
import { KitTabsThemeProps } from './types';

export const KitTabsTheme: KitTabsThemeProps = {
    extra: {
        borderRadius: 2,
        borderColor: theme.color.neutral.gray.gray300,
        padding: "4px 15px",
        height: 34,
        color: theme.color.neutral.typography.black,
        verticalMargin: 4,
        horizontalMargin: 16
    }
};