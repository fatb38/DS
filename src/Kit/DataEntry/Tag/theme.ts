import theme from '@theme/index';
import {KitTagTheme} from './types';

export const tagTheme: KitTagTheme = {
    fontSize: theme.typography.fontSize7,
    fontFamily: theme.typography.fontFamily,
    fontWeight: theme.typography.regularFontWeight,
    lineHeight: theme.typography.lineHeight7,
    height: '24px',
    placeholderFontSize: theme.typography.fontSize5,
    placeholderFontWeight: theme.typography.regularFontWeight,
    placeholderColor: theme.color.neutral.typography.black60
};
