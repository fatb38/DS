import {KitColorProp} from '@utils/functions/types';
import {CSSProperties} from 'react';

interface IKitSection {
    className?: string;
    style?: CSSProperties;
    borderColor?: KitColorProp;
}
