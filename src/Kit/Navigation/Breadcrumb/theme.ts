import theme from "@theme/index";

const BreadcrumbTheme = {
    fontSize: theme.typography.fontSize5,
    fontFamily: theme.typography.fontFamily,
    fontWeight: theme.typography.regularFontWeight,
    color: theme.color.neutral.gray.gray500,
    lastItemColor: theme.color.primary.blue400,
};

export default BreadcrumbTheme;
