import theme from "@theme/index";

const HeaderTheme = {
    color: theme.color.neutral.typography.black,
    backgroundColor: theme.color.neutral.typography.white,
    inputColor: theme.color.secondary.mediumGrey.mediumGrey500,
};

export default HeaderTheme;
