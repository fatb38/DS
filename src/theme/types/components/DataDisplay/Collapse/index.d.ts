export interface IKitCollapseTheme {
    colors: {
        background: {
            default: string;
            active: string;
        };
        border: {
            default: string;
            active: string;
        };
    };
    border: {
        radius: number;
    };
}
