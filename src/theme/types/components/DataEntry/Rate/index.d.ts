export interface IKitRateTheme {
    colors: {
        star: {
            default: string;
            disabled: string;
            active: {
                default: string;
                red: string;
                green: string;
                blue: string;
            };
        };
    };
}
