export interface IKitIconTheme {
    colors: {
        icon: {
            default: string;
            on: string;
        };
        background: {
            default: string;
            on: string;
        };
    };
    border: {
        radius: {
            default: number;
            on: number;
        };
    };
}
export interface IKitIconCssTokens {
    colors: {
        icon: {
            default: string;
            on: string;
        };
        background: {
            default: string;
            on: string;
        };
    };
    border: {
        radius: {
            default: string;
            on: string;
        };
    };
}
