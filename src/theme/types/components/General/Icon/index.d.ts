export interface IKitIconTheme {
    colors: {
        icon: {
            default: string;
            on: string;
        };
        background: {
            default: string;
            on: string;
        };
    };
    border: {
        radius: {
            default: number;
            on: number;
        };
    };
}
