export interface IKitHeaderTheme {
    colors: {
        background: {
            /**
             * default color
             * @TJS-type color
             */
            default: string;
        };
        typography: {
            input: {
                /**
                 * default color
                 * @TJS-type color
                 */
                default: string;
            };
        };
    };
}

export interface IKitHeaderCssTokens {
    colors: {
        background: {
            default: string;
        };
        typography: {
            input: {
                default: string;
            };
        };
    };
}
