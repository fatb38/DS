import {Meta, Canvas, Controls} from '@storybook/addon-docs';
import Gallery from './Template';
import CustomCanvas from '../../../../.storybook/components/CustomCanvas.jsx';

import Examples, {Sources} from './examples';
import DesignTokens from '../../../../.storybook/components/DesignTokens';
import fields from '../../../tokens';

import * as KitIconStories from './Icons.stories';

<Meta of={KitIconStories} />

<style>
    {`
        .gallery {
            margin: 40px 0;
        }

        .card {
            width: 210px;
            transition: color 0.3s ease-in-out,background-color 0.3s ease-in-out;
        }

        .icon-item {
            text-align: center;
            padding-bottom: .5rem;
            font-size: 36px;
            transition: transform 0.3s ease-in-out;
            will-change: transform;
        }

        .card:hover {
            background: #1677ff;
            color: white;
        }

        .card:hover .icon-item {
            transform: scale(1.4);
            transition: transform 0.3s ease-in-out;
            will-change: transform;
        }

        .icon-name {
            text-align: center;
            font-size: 12px;
        }
    `}
</style>

# Icon

Aristid Icon is a on/off state icon

## Examples

##### Basic

A basic Icon with on without `on` property.

<CustomCanvas content={Examples.Basic} source={Sources.Basic} id="icon-basic" />

##### Clickable

Icon can be clickable using `onClick` property.

<CustomCanvas content={Examples.Clickable} source={Sources.Clickable} id="icon-clickable" />

##### Color

Customized Icon with `color`property. If the given color is invalid, the default color will be applied.

<CustomCanvas content={Examples.Colors} source={Sources.Colors} id="icon-colors" />

## Api

<Canvas of={KitIconStories.Api} />

<Controls of={KitIconStories.Api} />

## Design Tokens

<DesignTokens path="components.Icon" tokens={fields?.components?.Icon} />

# Available icons

<Gallery />
