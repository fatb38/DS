import { Meta, Canvas, Controls, Story } from "@storybook/addon-docs";
import Gallery, {Template, argTypes} from './data';
import { KitIcon } from '@kit/General';
import CustomCanvas from '../../../../.storybook/components/CustomCanvas.jsx';

import Exemples, { Sources } from './exemples';

<Meta
    title="Design System/General/Icons"
    component={KitIcon}
    argTypes={argTypes}
/>

<style>
    {`
        .gallery {
            margin: 40px 0;
        }

        .card {
            width: 210px;
            transition: color 0.3s ease-in-out,background-color 0.3s ease-in-out;
        }

        .icon-item {
            text-align: center;
            padding-bottom: .5rem;
            font-size: 36px;
            transition: transform 0.3s ease-in-out;
            will-change: transform;
        }

        .card:hover {
            background: #1677ff;
            color: white;
        }

        .card:hover .icon-item {
            transform: scale(1.4);
            transition: transform 0.3s ease-in-out;
            will-change: transform;
        }

        .icon-name {
            text-align: center;
        }
    `}
</style>

# Icon

Aristid Icon is a on/off state icon

## Exemples

##### Basic

A basic Icon with on without `on` property.

<CustomCanvas content={Exemples.Basic} source={Sources.Basic} />

##### Hoverable

Show Icon with `on` property on hover state.

<CustomCanvas content={Exemples.Hoverable} source={Sources.Hoverable} />

##### Clickable

Icon can be clickable using `onClick` property.

<CustomCanvas content={Exemples.Clickable} source={Sources.Clickable} />

## Api

<Canvas>
    <Story name="Icon">{Template.bind({})}</Story>
</Canvas>

<Controls story="Icon"/>

# Available icons

<Gallery />

