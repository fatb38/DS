import { Meta, Canvas, Source } from "@storybook/addon-docs";
import { Controls } from "@storybook/addon-docs";
import CustomCanvas from '../../../../.storybook/components/CustomCanvas.jsx';
import Exemples, { Sources } from './exemples';
import * as KitBreadcrumbStories from './Breadcrumb.stories';

<Meta of={KitBreadcrumbStories} />

# Breadcrumb
A breadcrumb displays the current location within a hierarchy. It allows going back to states higher up in the hierarchy.

## Usage

- When the system has more than two layers in a hierarchy.
- When you need to inform the user of where they are.
- When the user may need to navigate back to a higher level.

## Examples

##### Basic
The simplest use.

<CustomCanvas content={Exemples.Basic} source={Sources.Basic} id="breadcrumb-basic" />

##### With an icon
The icon should be placed in front of the text.

<CustomCanvas content={Exemples.Icon} source={Sources.Icon} id="breadcrumb-icon" />

##### Configuring the separator
The separator can be customized by setting the separator property: `separator=">"`.

<CustomCanvas content={Exemples.Separator} source={Sources.Separator} id="breadcrumb-separator" />

##### Bread crumbs with drop down menu
Breadcrumbs support drop down menu.

<CustomCanvas content={Exemples.Dropdown} source={Sources.Dropdown} id="breadcrumb-dropdown" />

##### Configuring the Separator Independently
Customize separator for each other.

<CustomCanvas content={Exemples.IndependentSeparator} source={Sources.IndependentSeparator} id="breadcrumb-independent-separator" />

## API

### Breadcrumb

<Canvas of={KitBreadcrumbStories.Api} />

<Controls of={KitBreadcrumbStories.Api} />

### ItemType

> type ItemType = `RouteItemType` | `SeparatorType`

### RouteItemType

| Property | Description | Type | Default |
| --- | --- | --- | --- |
| className | The additional css class | string | - |
| dropdownProps | The dropdown props | `Dropdown` | - |
| href | Target of hyperlink | string | - |
| menu | The menu props | `MenuProps` | - |
| onClick | Set the handler to handle click event | (e:MouseEvent) => void | - |
| title | item name | ReactNode | - |

### SeparatorType

```ts
const item = {
  type: 'separator', // Must have
  separator: '/',
};
```

| Property  | Description       | Type        | Default |
| --------- | ----------------- | ----------- | ------- |
| type      | Mark as separator | `separator` |         |
| separator | Custom separator  | ReactNode   | `/`     |