import {Meta, Canvas, Source} from '@storybook/addon-docs';
import {Controls} from '@storybook/addon-docs';
import CustomCanvas from '../../../../.storybook/components/CustomCanvas.jsx';
import Examples, {Sources} from './examples';
import DesignTokens from '../../../../.storybook/components/DesignTokens';
import fields from '../../../tokens';
import * as KitDropdownStories from './DropDown.stories';

<Meta of={KitDropdownStories} />

# DropDown

A DropDown list.

## Usage

When there are more than a few options to choose from, you can wrap them in a `DropDown`. By hovering or clicking on the trigger, a DropDown menu will appear, which allows you to choose an option and execute the relevant action.

## Examples

##### Basic

The most basic DropDown menu.

<CustomCanvas content={Examples.Basic} source={Sources.Basic} id="dropdown-basic" />
##### Placement

Support 6 placements.

<CustomCanvas content={Examples.Placement} source={Sources.Placement} id="dropdown-placement" />

##### Arrow

You could display an arrow.

<CustomCanvas content={Examples.Arrow} source={Sources.Arrow} id="dropdown-arrow" />

##### Arrow pointing at the center

By specifying `arrow` prop with `{ pointAtCenter: true }`, the arrow will point to the center of the target element.

<CustomCanvas content={Examples.ArrowCenter} source={Sources.ArrowCenter} id="dropdown-arrow-center" />

##### Other elements

Divider and disabled menu item.

<CustomCanvas content={Examples.Disabled} source={Sources.Disabled} id="dropdown-disabled" />

##### Trigger mode

The default trigger mode is hover, you can change it to click.

<CustomCanvas content={Examples.Trigger} source={Sources.Trigger} id="dropdown-trigger" />

##### Custom dropdown

Customize the dropdown menu via `dropdownRender`. If you don't need the Menu content, use the Popover component directly.

<CustomCanvas content={Examples.Custom} source={Sources.Custom} id="dropdown-custom" />

##### Custom items

Customize the dropdown items via `label` prop on items.

<CustomCanvas content={Examples.CustomLabel} source={Sources.CustomLabel} id="dropdown-custom-label" />

##### Cascading menu

The menu has multiple levels.

<CustomCanvas content={Examples.CascadingMenu} source={Sources.CascadingMenu} id="dropdown-cascading-menu" />

##### Scrollable

Scroll when menu exceed height of 256px.

<CustomCanvas content={Examples.Scrollable} source={Sources.Scrollable} id="dropdown-scrollable" />

##### Context menu

The default trigger mode is `hover`, you can change it to `contextMenu`.

<CustomCanvas content={Examples.ContextMenu} source={Sources.ContextMenu} id="dropdown-context-menu" />

##### Selectable menu

Configure the `selectable` property in `menu` to enable selectable ability.

<CustomCanvas content={Examples.SelectableMenu} source={Sources.SelectableMenu} id="dropdown-selectable-menu" />

##### The way of hiding menu

The default is to close the menu when you click on menu items, this feature can be turned off.

<CustomCanvas content={Examples.HidingMenu} source={Sources.HidingMenu} id="dropdown-hiding-menu" />

## Api

<Canvas of={KitDropdownStories.Api} />

<Controls of={KitDropdownStories.Api} />

## Design Tokens

<DesignTokens path="components.DropDown" tokens={fields?.components?.DropDown} />
