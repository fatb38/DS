import { Meta, Story, Canvas, Source } from "@storybook/addon-docs";
import { Controls } from "@storybook/addon-docs";
import { KitDropDown } from "@kit/Navigation/";
import CustomCanvas from '../../../../.storybook/components/CustomCanvas.jsx';
import Exemples, { Sources } from './exemples';

import {
    argTypes,
    Template,
} from "./data";

<Meta
    title="Design System/Navigation/DropDown"
    component={KitDropDown}
    argTypes={argTypes}
/>

# DropDown

A DropDown list.

## Usage

When there are more than a few options to choose from, you can wrap them in a `DropDown`. By hovering or clicking on the trigger, a DropDown menu will appear, which allows you to choose an option and execute the relevant action.

## Examples

##### Basic

The most basic DropDown menu.

<CustomCanvas content={Exemples.Basic} source={Sources.Basic} />

##### Placement

Support 6 placements.

<CustomCanvas content={Exemples.Placement} source={Sources.Placement} />

##### Arrow

You could display an arrow.

<CustomCanvas content={Exemples.Arrow} source={Sources.Arrow} />

##### Arrow pointing at the center

By specifying `arrow` prop with `{ pointAtCenter: true }`, the arrow will point to the center of the target element.

<CustomCanvas content={Exemples.ArrowCenter} source={Sources.ArrowCenter} />

##### Other elements

Divider and disabled menu item.

<CustomCanvas content={Exemples.Disabled} source={Sources.Disabled} />

##### Trigger mode

The default trigger mode is hover, you can change it to click.

<CustomCanvas content={Exemples.Trigger} source={Sources.Trigger} />

##### Custom dropdown

Customize the dropdown menu via `dropdownRender`. If you don't need the Menu content, use the Popover component directly.

<CustomCanvas content={Exemples.Custom} source={Sources.Custom} />

##### Cascading menu

The menu has multiple levels.

<CustomCanvas content={Exemples.CascadingMenu} source={Sources.CascadingMenu} />

##### Context menu

The default trigger mode is `hover`, you can change it to `contextMenu`.

<CustomCanvas content={Exemples.ContextMenu} source={Sources.ContextMenu} />

##### Selectable menu

Configure the `selectable` property in `menu` to enable selectable ability.

<CustomCanvas content={Exemples.SelectableMenu} source={Sources.SelectableMenu} />

##### The way of hiding menu

The default is to close the menu when you click on menu items, this feature can be turned off.

<CustomCanvas content={Exemples.HidingMenu} source={Sources.HidingMenu} />

## Api

<Canvas>
    <Story name="DropDown">{Template.bind({})}</Story>
</Canvas>

<Controls story="DropDown" />
