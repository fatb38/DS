import { Meta, Canvas, Source } from "@storybook/addon-docs";
import { Controls } from "@storybook/addon-docs";
import CustomCanvas from '../../../../.storybook/components/CustomCanvas.jsx';
import Examples, { Sources } from './examples';
import DesignTokens from '../../../../.storybook/components/DesignTokens';
import fields from '../../../tokens';
import * as KitHeaderStories from './Header.stories';

<Meta of={KitHeaderStories} />

# Header
Header is displayed in top of the page. It allows showing, Title, breadcrumb, and some search and actions

## Usage

- When you need to inform the user of where they are.

## Examples

##### Basic
The simplest usage (empty header)

<CustomCanvas content={Examples.Basic} source={Sources.Basic} id="header-basic" />

##### With Title
The header can sho a Title. Just set the `title` prop

<CustomCanvas content={Examples.Title} source={Sources.Title} id="header-title" />

##### With search
It can also show a search field. just pass the `search`property with `Input` component props

<CustomCanvas content={Examples.Search} source={Sources.Search} id="header-search" />

##### With Breadcrumb
It can even display a breadcrumb with the `breadcrumb` property.

<CustomCanvas content={Examples.Breadcrumb} source={Sources.Breadcrumb} id="header-breadcrumd" />

##### With Plus actions
You can display a '+' button on the right, simply y setting the `onPlusClick` property

<CustomCanvas content={Examples.PlusAction} source={Sources.PlusAction} id="header-plus" />

##### With actions
Along with the '+' button, you can pass a set of actions. Actionscan be `Button` or `Select`. . Just set the `actions`property with an array of components.

<CustomCanvas content={Examples.Actions} source={Sources.Actions} id="header-actions" />

##### With all

Header with all options to display.

<CustomCanvas content={Examples.All} source={Sources.All} id="header-all" />


## API

<Canvas of={KitHeaderStories.Api} />

<Controls of={KitHeaderStories.Api} />


### Highlight Select options

When adding a select to the header options, you can set an option to be highlighted.
To do so, just add property `highlight` to true to the option to highlight

example: 

```jsx
[
    {
        value: 'view',
        icon: ControlOutlined,
        label: "Vue"
    },
    {
        value: 'save',
        icon: ControlOutlined,
        label: "Enregistrer la vue",
        highlight: true,
    }
];
```



## Design Tokens

<DesignTokens path="components.Header" tokens={fields?.components?.Header} />