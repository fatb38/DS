import { Meta, Story, Canvas } from "@storybook/addon-docs";
import { Controls } from "@storybook/addon-docs";
import { AutoCompleteArgTypes, Template} from "./data";
import Exemples, { Sources } from './exemples';
import CustomCanvas from '../../../../.storybook/components/CustomCanvas.jsx';

<Meta
    title="Design System/DataEntry/AutoComplete"
    argTypes={AutoCompleteArgTypes}
/>

<style>
    {`
        .certain-category-search-dropdown .ant-select-dropdown-menu-item-group-title {
            color: #666;
            font-weight: bold;
        }

        .certain-category-search-dropdown .ant-select-dropdown-menu-item-group {
            border-bottom: 1px solid #f6f6f6;
        }

        .certain-category-search-dropdown .ant-select-dropdown-menu-item {
            padding-left: 16px;
        }

        .certain-category-search-dropdown .ant-select-dropdown-menu-item.show-all {
            text-align: center;
            cursor: default;
        }

        .certain-category-search-dropdown .ant-select-dropdown-menu {
            max-height: 300px;
        }
    `}
</style>
# AutoComplete

Autocomplete function of input field.

## When To Use

- When you need an input box instead of a selector.
- When you need input suggestions or helping text.

The differences with Select are:

- AutoComplete is an input box with text hints, and users can type freely. The keyword is aiding **input**.
- Select is selecting among given choices. The keyword is **select**.

## Examples

##### Basic

Basic Usage, set data source of autocomplete with `options` property.

<CustomCanvas content={Exemples.Basic} source={Sources.Basic} />

##### Customized
You could set custom `Option` label

<CustomCanvas content={Exemples.Customized} source={Sources.Customized} />

##### Non-case-sensitive AutoComplete
A non-case-sensitive AutoComplete

<CustomCanvas content={Exemples.Noncasesensitive} source={Sources.Noncasesensitive} />

##### Lookup-Patterns - Certain Category
Demonstration of Lookup Patterns: Certain Category. Basic Usage, set options of autocomplete with options property.

<CustomCanvas content={Exemples.Certaincategory} source={Sources.Certaincategory} />

##### Lookup-Patterns - Uncertain Category
Demonstration of Lookup Patterns: Uncertain Category.

<CustomCanvas content={Exemples.Unertaincategory} source={Sources.Unertaincategory} />

##### Status
Add status to AutoComplete with `status`, which could be `error` or `warning`.

<CustomCanvas content={Exemples.Status} source={Sources.Status} />

##### Label and Helper

Add `label` and/or `helper` properties to give field context.

<CustomCanvas content={Exemples.Wrapper} source={Sources.Wrapper} />

## API

<Canvas>
    <Story name="AutoComplete">{Template.bind({})}</Story>
</Canvas>

<Controls story="AutoComplete" />