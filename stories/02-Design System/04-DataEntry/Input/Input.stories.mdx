import { Meta, Story, Canvas } from "@storybook/addon-docs";
import { Controls } from "@storybook/addon-docs";
import { InputArgTypes, Template } from "./data";
import Exemples, { Sources } from './exemples';
import CustomCanvas from '../../../../.storybook/components/CustomCanvas.jsx';

<Meta
    title="Design System/DataEntry/Input"
    argTypes={InputArgTypes}
/>

<style></style>

# Input

A basic widget for getting the user input is a text field. Keyboard and mouse can be used for providing or changing data.

## Usage

- A user input in a form field is needed.
- A search input is required.

## Examples

##### Basic

Basic usage example.

<CustomCanvas content={Exemples.Basic} source={Sources.Basic} />

##### Prefix and suffix

Add a prefix or suffix icons inside input.

<CustomCanvas content={Exemples.Prefixsuffix} source={Sources.Prefixsuffix} />

##### Password box

Input type of password.

<CustomCanvas content={Exemples.Password} source={Sources.Password} />

##### TextArea

For multi-line input.

<CustomCanvas content={Exemples.Textarea} source={Sources.Textarea} />

##### Autosizing the height to fit the content

`autoSize` prop for a `textarea` type of `Input` makes the height to automatically adjust based on the content. An option object can be provided to `autoSize` to specify the minimum and maximum number of lines the textarea will automatically adjust.

<CustomCanvas content={Exemples.Autosizing} source={Sources.Autosizing} />

##### With character counting

Show character counting.

<CustomCanvas content={Exemples.Counting} source={Sources.Counting} />

##### With clear icon

Input box with the remove icon, click the icon to delete everything.

<CustomCanvas content={Exemples.Clearicon} source={Sources.Clearicon} />

##### Status

Add status to Input with status, which could be error or warning.

<CustomCanvas content={Exemples.Status} source={Sources.Status} />

##### Label and Helper

Add `label` and/or `helper` properties to give field context.

<CustomCanvas content={Exemples.Wrapper} source={Sources.Wrapper} />

## Api

<Canvas>
    <Story name="Input">{Template.bind({})}</Story>
</Canvas>

<Controls story="Input" />