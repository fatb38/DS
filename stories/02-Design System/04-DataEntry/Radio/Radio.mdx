import { Meta, Canvas } from "@storybook/addon-docs";
import { Controls } from "@storybook/addon-docs";
import Examples, { Sources } from './examples';
import CustomCanvas from '../../../../.storybook/components/CustomCanvas.jsx';
import DesignTokens from '../../../../.storybook/components/DesignTokens';
import fields from '../../../tokens';
import * as KitRadioStories from './Radio.stories';

<Meta of={KitRadioStories} />

# Radio

## Usage
-   Used to select a single state from multiple options.
-   The difference from Select is that Radio is visible to the user and can facilitate the comparison of choice, which means there shouldn't be too many of them.

## Examples

##### Basic

The simplest use

<CustomCanvas content={Examples.Basic} source={Sources.Basic} id="radio-basic" />

##### Disabled

Radio unavailable.

<CustomCanvas content={Examples.Disabled} source={Sources.Disabled} id="radio-disabled" />

##### Danger Radio

Danger Radio

<CustomCanvas content={Examples.Danger} source={Sources.Danger} id="radio-danger" />

##### Radio Group

A group of radio components.

<CustomCanvas content={Examples.Group} source={Sources.Group} id="radio-group" />

##### Vertical Radio Group

Vertical Radio.Group, with more radios.

<CustomCanvas content={Examples.VerticalGroup} source={Sources.VerticalGroup} id="radio-vertical-group" />

##### Radio group with name

Passing the name property to all input[type="radio"] that are in the same Radio.Group. It is usually used to let the browser see your Radio.Group as a real "group" and keep the default behavior. For example, using left/right keyboard arrow to change your selection that in the same Radio.Group.

<CustomCanvas content={Examples.NamedGroup} source={Sources.NamedGroup} id="radio-named-group" />

## API

<Canvas of={KitRadioStories.Api} />

<Controls of={KitRadioStories.Api} />

## Methods

### Radio

| Name    | Description  |
| ------- | ------------ |
| blur()  | Remove focus |
| focus() | Get focus    |


## Design Tokens

<DesignTokens path="components.Radio" tokens={fields?.components?.Radio} />