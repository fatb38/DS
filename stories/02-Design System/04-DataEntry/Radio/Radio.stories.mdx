import { Meta, Story, Canvas } from "@storybook/addon-docs";
import { Controls } from "@storybook/addon-docs";
import { argTypes, Template } from "./data";
import Exemples, { Sources } from './exemples';
import CustomCanvas from '../../../../.storybook/components/CustomCanvas.jsx';

<Meta
    title="Design System/DataEntry/Radio"
    argTypes={argTypes}
/>

# Radio

## Usage
-   Used to select a single state from multiple options.
-   The difference from Select is that Radio is visible to the user and can facilitate the comparison of choice, which means there shouldn't be too many of them.

## Examples

##### Basic

The simplest use

<CustomCanvas content={Exemples.Basic} source={Sources.Basic} />

##### Disabled

Radio unavailable.

<CustomCanvas content={Exemples.Disabled} source={Sources.Disabled} />

##### Danger Radio

Danger Radio

<CustomCanvas content={Exemples.Danger} source={Sources.Danger} />

##### Radio Group

A group of radio components.

<CustomCanvas content={Exemples.Group} source={Sources.Group} />

##### Vertical Radio Group

Vertical Radio.Group, with more radios.

<CustomCanvas content={Exemples.VerticalGroup} source={Sources.VerticalGroup} />

##### Radio group with name

Passing the name property to all input[type="radio"] that are in the same Radio.Group. It is usually used to let the browser see your Radio.Group as a real "group" and keep the default behavior. For example, using left/right keyboard arrow to change your selection that in the same Radio.Group.

<CustomCanvas content={Exemples.NamedGroup} source={Sources.NamedGroup} />

## API

<Canvas>
    <Story name="Radio">{Template.bind({})}</Story>
</Canvas>

<Controls story="Radio" />

## Methods

### Radio

| Name    | Description  |
| ------- | ------------ |
| blur()  | Remove focus |
| focus() | Get focus    |
