import {Meta, Canvas} from '@storybook/addon-docs';
import {Controls} from '@storybook/addon-docs';
import Examples, {Sources} from './examples';
import CustomCanvas from '../../../../.storybook/components/CustomCanvas.jsx';
import DesignTokens from '../../../../.storybook/components/DesignTokens';
import fields from '../../../tokens';
import * as KitRateStories from './Rate.stories';

<Meta of={KitRateStories} />

# Rate

Rate component.

## Usage

-   Show evaluation.
-   A quick rating operation on something.

## Examples

##### Basic

The simplest usage.

<CustomCanvas content={Examples.Basic} source={Sources.Basic} id="rate-basic" />

##### Half star

Support select half star.

<CustomCanvas content={Examples.Half} source={Sources.Half} id="rate-half" />

##### Show copywriting

Add copywriting in rate components.

<CustomCanvas content={Examples.Copywriting} source={Sources.Copywriting} id="rate-copywriting" />

##### Read only

Read only, can't use mouse to interact.

<CustomCanvas content={Examples.Readonly} source={Sources.Readonly} id="rate-read-only" />

##### Clear star

Support set allow to clear star when click again.

<CustomCanvas content={Examples.Allowclear} source={Sources.Allowclear} id="rate-allow-clear" />

##### Rate Color

We preset a series of colorful Rate styles for use in different situations. If the given color is invalid, the default color will be applied.

<CustomCanvas content={Examples.Color} source={Sources.Color} id="rate-color" />

##### Other Character

Replace the default star to other icon. For correct rendering, you need to set these following properties : `defaultIcon`, `halfIcon` and `activeIcon`

<CustomCanvas content={Examples.Otherchar} source={Sources.Otherchar} id="rate-other-character" />

## API

<Canvas of={KitRateStories.Api} />

<Controls of={KitRateStories.Api} />

## Design Tokens

<DesignTokens path="components.Rate" tokens={fields?.components?.Rate} />
