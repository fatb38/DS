import { Meta, Story, Canvas } from "@storybook/addon-docs";
import { Controls } from "@storybook/addon-docs";
import { argTypes, Template } from "./data";
import Exemples, { Sources } from './exemples';
import CustomCanvas from '../../../../.storybook/components/CustomCanvas.jsx';

<Meta
    title="Design System/DataEntry/Slider"
    argTypes={argTypes}
/>

# Slider

Slider.

## Examples

##### TEST
Delete when finish.

<CustomCanvas content={Exemples.Test} source={Sources.Test} />

##### Basic
Basic slider. When `range` is `true`, display as dual thumb mode. When `disable` is `true`, the slider will not be interactable.

<CustomCanvas content={Exemples.Basic} source={Sources.Basic} />

##### Slider with InputNumber
Synchronize with KitInputNumber component.

<CustomCanvas content={Exemples.WithInputNumber} source={Sources.WithInputNumber} />

##### Slider with icon
You can add an icon beside the slider to make it meaningful.

<CustomCanvas content={Exemples.WithIcon} source={Sources.WithIcon} />


## API

<Canvas>
    <Story name="Slider">{Template.bind({})}</Story>
</Canvas>

<Controls story="Slider" />
