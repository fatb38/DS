import {Meta, Canvas} from '@storybook/addon-docs';
import {Controls} from '@storybook/addon-docs';
import Examples, {Sources} from './examples';
import CustomCanvas from '../../../../.storybook/components/CustomCanvas.jsx';
import DesignTokens from '../../../../.storybook/components/DesignTokens';
import fields from '../../../tokens';
import * as KitUploadStories from './Upload.stories';

<Meta of={KitUploadStories} />

## Usage

Upload file by selecting or dragging.

## When To Use

Uploading is the process of publishing information (web pages, text, pictures, video, etc.) to a remote server via a web page or upload tool.

-   When you need to upload one or more files.
-   When you need to show the process of uploading.
-   When you need to upload files by dragging and dropping.

## Examples

##### Upload by clicking

Classic mode. File selection dialog pops up when upload button is clicked.

<CustomCanvas content={Examples.UploadByClicking} source={Sources.UploadByClicking} id="upload-upload-click" />

##### Default Files

Use `defaultFileList` for uploaded files when page init.

<CustomCanvas content={Examples.DefaultFiles} source={Sources.DefaultFiles} id="upload-default-files" />

##### Avatar

Click to upload user's avatar, and validate size and format of picture with `beforeUpload`.

<CustomCanvas content={Examples.Avatar} source={Sources.Avatar} id="upload-avatar" />

##### Pictures Wall

After users upload picture, the thumbnail will be shown in list. The upload button will disappear when count meets limitation.

<CustomCanvas content={Examples.PicturesWall} source={Sources.PicturesWall} id="upload-pictures-wall" />

##### Crop image before uploading

Use `antd-img-crop` to crop image before uploading.

<CustomCanvas content={Examples.Crop} source={Sources.Crop} id="upload-crop" />

##### Pictures with list style

If uploaded file is a picture, the thumbnail can be shown. `IE8/9` do not support local thumbnail show. Please use `thumbUrl` instead.

<CustomCanvas content={Examples.ListStyle} source={Sources.ListStyle} id="upload-list-style" />

##### Drag and Drop

You can drag files to a specific area, to upload. Alternatively, you can also upload by selecting.

We can upload serveral files at once in modern browsers by giving the input the `multiple` attribute.

<CustomCanvas content={Examples.DragNDrop} source={Sources.DragNDrop} id="upload-dnd" />

##### Complete control over file list

You can gain full control over filelist by configuring `fileList`.

You can accomplish all kinds of customed functions. The following shows two circumstances:

-   limit the number of uploaded files.
-   read from response and show file link.

<CustomCanvas content={Examples.CompleteControl} source={Sources.CompleteControl} id="upload-complete-control" />

##### Upload directory

You can select and upload a whole directory.

<CustomCanvas content={Examples.Directory} source={Sources.Directory} id="upload-directory" />

##### Upload png file only

`beforeUpload` only prevent upload behavior when return false or reject promise, the prevented file would still show in file list. Here is the example you can keep prevented files out of list by using `onChange`.

<CustomCanvas content={Examples.PngOnly} source={Sources.PngOnly} id="uploadpng-only" />

##### Customize preview file

Customize local preview. Can handle with non-image format files such as video.

<CustomCanvas content={Examples.CustomPreview} source={Sources.CustomPreview} id="upload-custom-preview" />

##### Aliyun OSS

Use Aliyun OSS upload example.

<CustomCanvas content={Examples.AliyunOSS} source={Sources.AliyunOSS} id="upload-aliyun-oss" />

##### Upload manually

Upload files manually after `beforeUpload` returns `false`.

<CustomCanvas content={Examples.Manual} source={Sources.Manual} id="upload-manual" />

##### Transform file before request

Use `transformFile` for transform file before request such as add a watermark.

<CustomCanvas content={Examples.Transform} source={Sources.Transform} id="upload-transform" />

##### Custom action icon

Use `showUploadList` for custom action icons of files.

<CustomCanvas content={Examples.CustomActionIcon} source={Sources.CustomActionIcon} id="upload-custom-action-icon" />

##### Customize Progress Bar

Use `progress` for customize progress bar.

<CustomCanvas content={Examples.CustomProgress} source={Sources.CustomProgress} id="upload-custom-progress" />

## API

<Canvas of={KitUploadStories.Api} />

<Controls of={KitUploadStories.Api} />

## Design Tokens

<DesignTokens path="components.Upload" tokens={fields?.components?.Upload} />
