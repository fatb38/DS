import { Meta, Canvas } from "@storybook/addon-docs";
import { Controls } from "@storybook/addon-docs";
import Exemples, { Sources } from './exemples';
import CustomCanvas from '../../../../.storybook/components/CustomCanvas.jsx';
import *  as KitAvatarStories from './Avatar.stories';

<Meta of={KitAvatarStories} />

# Avatar

Avatars can be used to represent people or objects. It supports images, `Icon`s, or letters.

## Usage

Obviously (🤭) when you need to display an avatar.

## Examples

##### Basic

Three sizes and two shapes are available.

<CustomCanvas content={Exemples.Basic} source={Sources.Basic} id="avatar-basic" />

##### Type

Image, Icon, letter and text are supported.

<CustomCanvas content={Exemples.Type} source={Sources.Type} id="avatar-type" />

##### Color

Letter and text can have colors and background colors.

<CustomCanvas content={Exemples.Color} source={Sources.Color} id="avatar-color" />

##### Avatar.Group

Avatar group display.

<CustomCanvas content={Exemples.Group} source={Sources.Group} id="avatar-group" />

##### With Badge

Usually used for reminders and notifications.

<CustomCanvas content={Exemples.Badge} source={Sources.Badge} id="avatar-badge" />

##### Responsive Size

Avatar size can be automatically adjusted based on the screen size.

<CustomCanvas content={Exemples.Responsive} source={Sources.Responsive} id="avatar-responsive" />

## Api

<Canvas of={KitAvatarStories.Api} />

<Controls of={KitAvatarStories.Api} />