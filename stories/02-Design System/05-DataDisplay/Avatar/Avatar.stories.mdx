import { Meta, Story, Canvas } from "@storybook/addon-docs";
import { Controls } from "@storybook/addon-docs";
import { AvatarArgTypes, Template } from "./data";
import Exemples, { Sources } from './exemples';
import CustomCanvas from '../../../../.storybook/components/CustomCanvas.jsx';

<Meta
    title="Design System/DataDisplay/Avatar"
    argTypes={AvatarArgTypes}
/>

# Avatar

Avatars can be used to represent people or objects. It supports images, `Icon`s, or letters.

## Usage

Obviously (🤭) when you need to display an avatar.

## Examples

##### Basic

Three sizes and two shapes are available.

<CustomCanvas content={Exemples.Basic} source={Sources.Basic} />

##### Type

Image, Icon, letter and text are supported.

<CustomCanvas content={Exemples.Type} source={Sources.Type} />

##### Color

Letter and text can have colors and background colors.

<CustomCanvas content={Exemples.Color} source={Sources.Color} />

##### Avatar.Group

Avatar group display.

<CustomCanvas content={Exemples.Group} source={Sources.Group} />

##### With Badge

Usually used for reminders and notifications.

<CustomCanvas content={Exemples.Badge} source={Sources.Badge} />

##### Responsive Size

Avatar size can be automatically adjusted based on the screen size.

<CustomCanvas content={Exemples.Responsive} source={Sources.Responsive} />

## Api

<Canvas>
    <Story name="Avatar">{Template.bind({})}</Story>
</Canvas>

<Controls story="Avatar" />