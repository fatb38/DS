import { Meta, Story, Canvas } from "@storybook/addon-docs";
import { Controls } from "@storybook/addon-docs";
import { argTypes, Template } from "./data";
import Exemples, { Sources } from './exemples';
import CustomCanvas from '../../../../.storybook/components/CustomCanvas.jsx';
import * as KitCardStories from './Card.stories';

<Meta of={KitCardStories} />

# Card

todo

## Examples

##### Basic
Exemple description

<CustomCanvas content={Exemples.Basic} source={Sources.Basic} id="template-basic"/>

##### Card with image
Card with an image

<CustomCanvas content={Exemples.WithImage} source={Sources.WithImage} id="template-with-image"/>

##### Card with content
Card with a content

<CustomCanvas content={Exemples.WithContent} source={Sources.WithContent} id="template-with-content"/>

##### Card with content
Card, extra and actions can be disabled 

<CustomCanvas content={Exemples.Disabled} source={Sources.Disabled} id="template-disabled"/>

## Api

<Canvas of={KitCardStories.Api} />

<Controls of={KitCardStories.Api} />
