import { Meta, Story, Canvas } from "@storybook/addon-docs";
import { Controls } from "@storybook/addon-docs";
import Examples, { Sources } from './examples';
import CustomCanvas from '../../../../.storybook/components/CustomCanvas.jsx';
import DesignTokens from '../../../../.storybook/components/DesignTokens';
import fields from '../../../tokens';
import * as KitCardStory from './Card.stories';

<Meta of={KitCardStory} />

# Card

Component to display information with image and actions.

## Examples

##### Basic
This is a basic card with a preview and a title.

<CustomCanvas content={Examples.Basic} source={Sources.Basic} id="card-basic"/>

##### No preview
Card with the default preview.

<CustomCanvas content={Examples.NoPreview} source={Sources.NoPreview} id="card-no-preview"/>

##### Complete card
This is an exemple with all cards props.

<CustomCanvas content={Examples.Full} source={Sources.Full} id="card-full"/>

##### Disabled card
This is an exemple with `disabled` props `true`.

<CustomCanvas content={Examples.Disabled} source={Sources.Disabled} id="card-disabled"/>

## Api

<Canvas of={KitCardStory.Api} />

<Controls of={KitCardStory.Api} />

## Design Tokens

<DesignTokens path="components.Card" tokens={fields?.components?.Card} />