import { Meta, Story, Canvas } from "@storybook/addon-docs";
import { Controls } from "@storybook/addon-docs";
import { CardArgTypes, Template } from "./data";
import Exemples, { Sources } from './exemples';
import CustomCanvas from '../../../../.storybook/components/CustomCanvas.jsx';

<Meta
    title="Design System/DataDisplay/Card"
    argTypes={CardArgTypes}
/>

# Card

A component to display product informations

## Usage

The Card is used to display a product identity Card - some basic informations - such as a preview, label colors and so on.
It's also provides ability to select and / or edit product data

## Examples

##### Basic
The simplest usage.

###### Horizontal layout (default)

<CustomCanvas content={Exemples.Basic} source={Sources.Basic} />

###### Vertical layout

<CustomCanvas content={Exemples.Vertical} source={Sources.Vertical} />

##### With icon
display an icon instead of image

<CustomCanvas content={Exemples.Icon} source={Sources.Icon} />

##### With Avatar
display an Avatar

<CustomCanvas content={Exemples.Avatar} source={Sources.Avatar} />

##### With Tags
add tags in the Card

<CustomCanvas content={Exemples.Tags} source={Sources.Tags} />


##### With colors
show colors bar near image

<CustomCanvas content={Exemples.Colors} source={Sources.Colors} />

##### Selectable
set the card selectable. Simply provide the `onSelectChange` property to enable selection.

<CustomCanvas content={Exemples.Selectable} source={Sources.Selectable} />

##### Editable
allow to edit the item assicated to the card. Simply provide the `onEdit` property to enable edition.

<CustomCanvas content={Exemples.Editable} source={Sources.Editable} />

##### With actions
add actions near select checkbox.

<CustomCanvas content={Exemples.Actions} source={Sources.Actions} />

##### Disabled
Card element in disabled state.

<CustomCanvas content={Exemples.Disabled} source={Sources.Disabled} />

## Api

<Canvas>
    <Story name="Card">{Template.bind({})}</Story>
</Canvas>

<Controls story="Card" />