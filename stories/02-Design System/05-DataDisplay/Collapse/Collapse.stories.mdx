import { Meta, Story, Canvas } from "@storybook/addon-docs";
import { Controls } from "@storybook/addon-docs";
import { CollapseArgTypes, Template } from "./data";
import Exemples, { Sources } from './exemples';
import CustomCanvas from '../../../../.storybook/components/CustomCanvas.jsx';

<Meta
    title="Design System/DataDisplay/Collapse"
    argTypes={CollapseArgTypes}
/>

# Collapse

A content area which can be collapsed and expanded.

## Usage

- Can be used to group or hide complex regions to keep the page clean.
- `Accordion` is a special kind of `Collapse`, which allows only one panel to be expanded at a time.

## Examples

##### Basic

By default, any number of panels can be expanded at a time. The first panel is expanded in this example.

<CustomCanvas content={Exemples.Basic} source={Sources.Basic} />

##### Size

If a large or small collapse is desired, set the `size` property to either `large` or `small` respectively. Omit the `size` property for a collapse with the default size.

<CustomCanvas content={Exemples.Size} source={Sources.Size} />

##### Accordion

In accordion mode, only one panel can be expanded at a time.

<CustomCanvas content={Exemples.Accordion} source={Sources.Accordion} />

##### Nested panel

`Collapse` is nested inside the `Collapse`.

<CustomCanvas content={Exemples.Nested} source={Sources.Nested} />

##### Custom header content

Render header content with a proper layout in the top-left corner of each panel. To do so, you can use `KitCollapse.Header`.

<CustomCanvas content={Exemples.CustomHeaderContent} source={Sources.CustomHeaderContent} />

##### Custom header extra actions

Render extra element in the top-right corner of each panel. To do so, you can use `KitCollapse.HeaderExtra`.

<CustomCanvas content={Exemples.CustomHeaderExtra} source={Sources.CustomHeaderExtra} />

##### Collapsible

Specify the trigger area of collapsible by `collapsible`.

<CustomCanvas content={Exemples.Collapsible} source={Sources.Collapsible} />

## API

<Canvas withSource="none">
    <Story name="Collapse">{Template.bind({})}</Story>
</Canvas>

<Controls story="Collapse" />