import {Meta, Story, Canvas} from '@storybook/addon-docs';
import {Controls} from '@storybook/addon-docs';
import Examples, {Sources} from './examples';
import CustomCanvas from '../../../../.storybook/components/CustomCanvas.jsx';
import DesignTokens from '../../../../.storybook/components/DesignTokens';
import fields from '../../../tokens';
import * as KitEmptyStories from './Empty.stories';

<Meta of={KitEmptyStories} />

# Empty

Empty state placeholder.

## Usage

-   When there is no data provided, display for friendly tips.

-   User tutorial to create something in fresh new situation.

## Examples

##### Basic

Simplest Usage.

<CustomCanvas content={Examples.Basic} source={Sources.Basic} id="component-empty" />

##### Choose image

You can choose another style of `image` by setting image property.

<CustomCanvas content={Examples.ChooseImage} source={Sources.ChooseImage} id="component-choose-image" />

##### Set title and description

You can set `title` and `description` to give more information.

<CustomCanvas content={Examples.TitleDescription} source={Sources.TitleDescription} id="component-title-description" />

##### Add button

You can add a `button` to guide user to do something.

<CustomCanvas content={Examples.Button} source={Sources.Button} id="component-button" />

##### Customize

You can fully customize the component as all props accept a ReactNode.

<CustomCanvas content={Examples.Custom} source={Sources.Custom} id="component-custom" />

## Api

<Canvas of={KitEmptyStories.Api} />

<Controls of={KitEmptyStories.Api} />

## Translation Tokens

|        | en-US   | fr-FR       |
| ------ | ------- | ----------- |
| noData | No Data | Aucune data |
