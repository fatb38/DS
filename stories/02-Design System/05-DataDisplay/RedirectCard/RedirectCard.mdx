import {Meta, Story, Canvas} from '@storybook/addon-docs';
import {Controls} from '@storybook/addon-docs';
import Examples, {Sources} from './examples';
import CustomCanvas from '../../../../.storybook/components/CustomCanvas.jsx';
import DesignTokens from '../../../../.storybook/components/DesignTokens';
import fields from '../../../tokens';
import * as KitRedirectCardStory from './RedirectCard.stories';

<Meta of={KitRedirectCardStory} />

# RedirectCard

Component to display informations and redirect user (redirection is managed outside of Design System).

## Examples

### Basic

This is a basic redirect card with a title and a description.

<CustomCanvas content={Examples.Basic} source={Sources.Basic} id="redirect-card-basic" />

### With Icon

You can display the redirect card with an icon. To do this, you need to pass the icon to the `icon` property. If `coverSrc` is set, it will be displayed instead of the icon.

<CustomCanvas content={Examples.Icon} source={Sources.Icon} id="redirect-card-icon" />

### With Cover

You can display the redirect card with a cover. To do this, you need to pass the image to the `coverSrc` property.

<CustomCanvas content={Examples.Cover} source={Sources.Cover} id="redirect-card-cover" />

### With Actions Buttons

You can display up to three action buttons. To do this, you need to pass buttons to the `actions` property.

<CustomCanvas content={Examples.Actions} source={Sources.Actions} id="redirect-card-actions" />

### Disabled

RedirectCard can be disabled by passing the `disabled` property.

<CustomCanvas content={Examples.Disabled} source={Sources.Disabled} id="redirect-card-disabled" />

## API

<Canvas of={KitRedirectCardStory.Api} />

<Controls of={KitRedirectCardStory.Api} />
