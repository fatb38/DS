import { Meta, Canvas } from "@storybook/addon-docs";
import { Controls } from "@storybook/addon-docs";
import Examples, { Sources } from './examples';
import CustomCanvas from '../../../../.storybook/components/CustomCanvas.jsx';
import DesignTokens from '../../../../.storybook/components/DesignTokens';
import fields from '../../../tokens';
import * as KitTootlipStories from './Tooltip.stories';

<Meta of={KitTootlipStories} />

# Tooltip

A simple text popup tip.

## Usage

- The tip is shown on mouse enter, and is hidden on mouse leave. The Tooltip doesn't support complex text or operations.

- To provide an explanation of a `button/text/operation`. It's often used instead of the html title attribute.

## Examples

##### Basic

The simplest usage.

<CustomCanvas content={Examples.Basic} source={Sources.Basic} id="tooltip-basic" />

##### Placement

There are 12 placement options available.

<CustomCanvas content={Examples.Placement} source={Sources.Placement} id="tooltip-placement" />

##### Arrow

Support show, hide or keep arrow in the center.

<CustomCanvas content={Examples.Arrow} source={Sources.Arrow} id="tooltip-arrow" />

## Api

<Canvas of={KitTootlipStories.Api} />

<Controls of={KitTootlipStories.Api} />


## Design Tokens

<DesignTokens path="components.Tooltip" tokens={fields?.components?.Tooltip} />