import { Meta, Story, Canvas } from "@storybook/addon-docs";
import { Controls } from "@storybook/addon-docs";
import { argTypes, Template } from "./data";
import Exemples, { Sources } from './exemples';
import CustomCanvas from '../../../../.storybook/components/CustomCanvas.jsx';
import DesignTokens from '../../.storybook/components/DesignTokens';
import fields from '../tokens';
import * as Template from './Template.stories';

<Meta of={KitCardStories} />

# Template

Template's description

## Examples

##### Basic
Exemple description

<CustomCanvas content={Exemples.Basic} source={Sources.Basic} id="template-basic"/>

## Api

<Canvas of={Template.Api} />

<Controls of={Template.Api} />

## Design Tokens

<DesignTokens path="components.Template" tokens={fields?.components?.Template} />